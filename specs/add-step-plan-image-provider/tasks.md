# Implementation Tasks

1. [x] Implement generic dashboard support for the specified model `paramConfig` schema: resolve fields in `params` order, shallow-merge global definitions with model overrides while replacing arrays wholesale, support select/text/number/boolean fields and decimal steps, render `min === max` number fields as disabled fixed values, and reset values to resolved defaults when the selected model changes.
2. [x] Refactor request-body construction so only resolved fields declared by the selected model are submitted; extract a pure helper in the approved component directory if needed for direct tests.
3. [x] Add the `step-plan` registry entry with API-key identity and acquisition URL, image-only service discovery, `step-image-edit-2`, generation and GET validation endpoints, exact `params`/`paramConfig` metadata, and no edit capability.
4. [x] Update `open-sse/providers/registry/index.js` directly because this checkout has no working generator: retain all existing entries in their current relative order, append Step Plan with the next contiguous import identifier, append its matching export, and do not move or renumber existing imports.
5. [x] Add and register a dedicated Step Plan image adapter that builds Bearer-authenticated requests, validates the complete documented request contract, preserves explicit falsey values, and returns the OpenAI-like response unchanged.
6. [x] Add runtime adapter/core tests for the endpoint, headers, defaults, every supported field, unsupported-field filtering, all validation boundaries, URL/base64 response preservation, URL/base64-to-binary conversion, invalid-JSON 2xx responses, and representative upstream failures.
7. [x] Extend generic media credential validation to honor configured validation URL and method, then configure Step Plan to validate with an authenticated GET to `/step_plan/v1/models`.
8. [x] Add validation-route tests proving `validateUrl` is the sole probe target with no fallback; Step Plan accepts only 2xx and does not accept 401/403, other non-2xx, timeout, or network failures; and providers without validation metadata retain the legacy non-401/403 acceptance behavior.
9. [x] Add dashboard-focused tests proving the default and advanced Step Plan requests use the exact supported fields and constraints; options arrays replace global arrays; boolean and disabled fixed-number controls work; model switching resets values; and `black-forest-labs/flux-pro-1.1` retains `n`/`size` while no longer sending undeclared global defaults.
10. [x] Add provider creation/discovery and registry consistency tests proving `step-plan` is eligible for generic API-key connections, its model resolves under the image kind, and every published Step Plan image model has an adapter.
11. [x] Create `tests/unit/image-generation-handler.test.js` with `vi.mock` scaffolding for handler dependencies including auth credential functions, `getSettings`, model/combo resolution, token refresh, combo handling, logger behavior, and the image core; test local API-key enforcement, preferred Step Plan connection selection, retryable multi-account fallback, combo compatibility, response pass-through, and `?response_format=binary` propagation as `binaryOutput: true` without live upstream requests. Actual binary conversion remains a core-test responsibility in task 6.
12. [x] Update only the alias/provider snapshots affected by the intentional provider and model addition, and run the repository's no-regression baseline checks.
13. [x] Run targeted image, handler, provider-creation, provider-validation, dashboard, registry, lint, and baseline verification; fix regressions while keeping image editing and unrelated image-core hardening out of scope.
